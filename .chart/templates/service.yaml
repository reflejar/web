apiVersion: v1
kind: Service
metadata:
  name: {{ include "reflejar.fullname" . }}
  labels:
    {{- include "reflejar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reflejar.selectorLabels" . | nindent 4 }}
