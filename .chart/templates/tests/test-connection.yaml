apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reflejar.fullname" . }}-test-connection"
  labels:
    {{- include "reflejar.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reflejar.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
